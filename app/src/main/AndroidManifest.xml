<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cursofirebase">
    <!-- nosso aplicativo terá acesso a internet -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:label="Recuperar senha"
            android:name=".Activity.RecuperarSenha"
            android:parentActivityName=".Activity.MainActivity"
            ></activity>

        <activity  android:label="Cardápio"
            android:name=".Activity.CardapioActivity"
            android:parentActivityName=".Activity.PrincipalActivity"/>

        <activity android:label="Cadastro Usuário"
            android:name=".Activity.CadastroUsuarioComumActivity"
            android:parentActivityName=".Activity.MainActivity"/>

        <activity android:name=".Activity.PrincipalActivity"
            android:label="Home"/>

        <activity android:label="Cadastro Usuário"
            android:name=".Activity.CadastroUsuarioActivity"
            android:parentActivityName=".Activity.PrincipalActivity"/>

        <activity
            android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application> <!-- autorizando o app acessar arquivos externos a ele, dentro do nosso android -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>